[package]
name = "gated-sorter-audit"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
pathagon-engine = { package = "pathagon-engine", path = "../../pathagon/engine-rs", features = ["inference"] }
serde_json = "1"
